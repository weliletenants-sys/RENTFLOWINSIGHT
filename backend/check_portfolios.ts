import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { const user = await prisma.profiles.findFirst({where:{email:'rnampiima@gmail.com'}}); if(!user) return; const ports = await prisma.investorPortfolios.findMany({where:{investor_id:user.id}}); console.log(ports.map(p => ({ id: p.id, created: p.created_at, next: p.next_roi_date }))); } run();
